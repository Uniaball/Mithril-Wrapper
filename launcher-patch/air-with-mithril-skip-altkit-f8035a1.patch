diff --git a/Natives/CMakeLists.txt b/Natives/CMakeLists.txt
index d6bf937..2e3d247 100644
--- a/Natives/CMakeLists.txt
+++ b/Natives/CMakeLists.txt
@@ -522,10 +522,10 @@ target_link_libraries(AngelAuraAmethyst
   "-F'${CMAKE_CURRENT_LIST_DIR}/build'"
   "-F'${CMAKE_CURRENT_LIST_DIR}/resources/Frameworks'"
   # ZeroTier 暂时移除："${ZT_FRAMEWORK_SEARCH_PATH}" "${ZT_FRAMEWORK_LINK}" "${ZT_CXX_LIB}"
-  "-framework AltKit"
+  # AltKit/CAltKit removed: prebuilt device-only frameworks break the simulator link
+  # ("built for iOS vs iOS-simulator"). JIT enablement is not needed for CI mithril test.
   "-framework AuthenticationServices"
   "-framework AVFoundation"
-  "-framework CAltKit"
   "-framework CoreFoundation"
   "-framework CoreGraphics"
   "-framework CoreImage"
